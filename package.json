{
  "name": "localaudit-saas",
  "private": true,
  "workspaces": [
    "apps/web",
    "workers"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:worker": "npm --workspace workers run dev",
    "build:web": "npm --workspace apps/web run build",
    "build:worker": "npm --workspace workers run build"
  }
}
